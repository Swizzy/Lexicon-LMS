@model IEnumerable<LexiconLMS.Models.Module>

@{
    ViewBag.Title = "Course Modules";
}

@*<h3>@Html.DisplayFor(modelItem => item.Course.Name)</h3>*@
<h3>.NET Vår 2017</h3>
<h4>Course Modules</h4>

<p>
    @Html.ActionLink("Add Module", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
      @*<th>@Html.DisplayNameFor(model => model.Activities.StartDate)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.Activities.EndDate)
    </th>*@
    <th>
        Activities
    </th>
    <th>
        Module Documents
    </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        @*<td>
        @Html.DisplayFor(modelItem => item.Activities.StartDate.First())
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Activities.EndDate.Last())
    </td>*@
        <td>
            @Html.ActionLink("Modify", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            @Html.ActionLink("Activities", "Details", new { id = item.CourseId, item.Id }) |
            @Html.ActionLink("Documents", "Documents", new { id = item.CourseId, item.Id }) |
        </td>
    </tr>
}

</table>
